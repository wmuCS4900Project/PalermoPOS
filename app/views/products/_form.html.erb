<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ProductName %>
    <%= f.text_field :ProductName %>
  </div>

  <div class="field">
    <%= f.label :Cost %>
    <%= f.text_field :Cost %>
  </div>

  <div class="field">
    <%= f.label :ProductCategory %>
    <%= f.number_field :ProductCategory %>
  </div>

  <div class="field">
    <%= f.label :Generic %>
    <%= f.check_box :Generic %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
