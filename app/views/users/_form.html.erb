<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :UserName %>
    <%= f.text_field :UserName %>
  </div>

  <div class="field">
    <%= f.label :Password %>
    <%= f.text_field :Password %>
  </div>

  <div class="field">
    <%= f.label :Driver %>
    <%= f.check_box :Driver %>
  </div>

  <div class="field">
    <%= f.label :DateAdded %>
    <%= f.datetime_select :DateAdded %>
  </div>

  <div class="field">
    <%= f.label :IsManager %>
    <%= f.check_box :IsManager %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
